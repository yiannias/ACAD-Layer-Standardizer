The "Purge" Button is not displaying right, and it seems that the file I am testing the app on has MANY empty layers (121).  This is a problem because "Purge" Pulls up a VERY tall window with all of the layer names, but it is taller than my screen and I can't close it. Change this window to one that can display about 35 lines, and when the list is longer, gives you a vertical scroll bar. That same window should be "dark" in the same way that the main app widow is: Black Title bar, Black content background and light grey text.
1:58 AM
Thought: 3.8s
The user is talking about the MessageBox that appears when clicking "Purge" — it shows all the empty layer names in a list. When there are 121 layers, the message box becomes enormous. I need to replace the MessageBox with a custom WPF window that:
1. Shows the list with a scrollbar (about 35 lines visible)
2. Dark theme (black title bar, black background, light grey text)
3. Has Yes/No buttons (or equivalent) for confirmation

Let me look at the current purge confirmation code in NodeGraphWindow.xaml.cs:

var msg = $"Purge {emptyLayers.Count} unused layer(s)?\n\n{string.Join("\n", emptyLayers.OrderBy(n => n))}";
if (MessageBox.Show(msg, "Purge Unused Layers",
        MessageBoxButton.YesNo, MessageBoxImage.Warning) != MessageBoxResult.Yes)
    return;

I need to create a custom dialog window. Let me think about the approach:

1. Create a new WPF Window class, e.g., PurgeConfirmDialog.xaml + .xaml.cs
2. It takes a list of layer names
3. Shows them in a scrollable ListBox
4. Has Confirm/Cancel buttons
5. Dark theme styling

Actually, I can make a simpler approach - just create the window in code-behind or as a small inline dialog. But a proper XAML window is cleaner.

Let me create PurgeConfirmDialog.xaml and PurgeConfirmDialog.xaml.cs.

The dialog needs:
- Window: dark background, dark title bar
- A TextBlock with the prompt and count
- A ScrollViewer with a ListBox of layer names (~35 lines visible)
- Confirm and Cancel buttons at the bottom
