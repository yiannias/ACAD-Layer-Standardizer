<!-- Shared dark theme for every window/dialog in the plugin (chris,
     2026-07-11: "some windows are light and some are dark; they should all
     coordinate"). Merge into a window's Resources:

       <ResourceDictionary.MergedDictionaries>
         <ResourceDictionary Source="pack://application:,,,/AcLayerStandardizer;component/UI/DarkTheme.xaml" />
       </ResourceDictionary.MergedDictionaries>

     Implicit styles here (Button, ListView, ...) apply to the window's own
     visual tree automatically. Context menus are popups OUTSIDE that tree,
     so implicit styles never reach them: the keyed DarkContextMenu /
     DarkMenuItem / DarkMenuSeparator styles must be assigned explicitly
     (see NodeGraphWindow's placeholder menus and AddCheckableItem).
     Pair with WindowTheming.EnableDarkTitleBar for the window chrome. -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- Palette: same surfaces as NodeGraphWindow / PurgeConfirmDialog /
       TemplateSwitchDialog so all windows read as one app. -->
  <SolidColorBrush x:Key="DarkWindowBrush" Color="#1a1a1a" />
  <SolidColorBrush x:Key="DarkPanelBrush" Color="#242424" />
  <SolidColorBrush x:Key="DarkControlBrush" Color="#3a3a3a" />
  <SolidColorBrush x:Key="DarkBorderBrush" Color="#555555" />
  <SolidColorBrush x:Key="DarkTextBrush" Color="#cccccc" />
  <SolidColorBrush x:Key="DarkSubtleTextBrush" Color="#999999" />
  <SolidColorBrush x:Key="DarkAccentBrush" Color="#4682b4" />

  <!-- Dark button, same visual language as NodeGraphWindow's ModernButton
       (kept separate so that window's keyed style stays untouched). -->
  <Style TargetType="Button">
    <Setter Property="Background" Value="{StaticResource DarkControlBrush}" />
    <Setter Property="Foreground" Value="#eeeeee" />
    <Setter Property="BorderBrush" Value="{StaticResource DarkBorderBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="12,5" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="5"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="#4a4a4a" />
              <Setter Property="BorderBrush" Value="#888888" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Background" Value="#2a2a2a" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.45" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="CheckBox">
    <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}" />
  </Style>

  <!-- ListView/GridView (PreviewDialog): dark list surface, flat dark
       column headers (the default header chrome is a light gradient). -->
  <Style TargetType="ListView">
    <Setter Property="Background" Value="#202020" />
    <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}" />
    <Setter Property="BorderBrush" Value="#444444" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>

  <Style TargetType="ListViewItem">
    <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="4,3" />
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="#2e2e2e" />
      </Trigger>
      <Trigger Property="IsSelected" Value="True">
        <Setter Property="Background" Value="#33517a" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style TargetType="GridViewColumnHeader">
    <Setter Property="Background" Value="#2a2a2a" />
    <Setter Property="Foreground" Value="{StaticResource DarkSubtleTextBrush}" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="GridViewColumnHeader">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="#444444"
                  BorderThickness="0,0,1,1"
                  Padding="6,5">
            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Context menu chrome. Keyed, not implicit: menu popups live outside
       the window's visual tree and never see implicit window styles. -->
  <Style x:Key="DarkContextMenu" TargetType="ContextMenu">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Background" Value="{StaticResource DarkPanelBrush}" />
    <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ContextMenu">
          <Border Background="{StaticResource DarkPanelBrush}"
                  BorderBrush="{StaticResource DarkBorderBrush}"
                  BorderThickness="1"
                  CornerRadius="6"
                  Padding="4">
            <StackPanel IsItemsHost="True"
                        KeyboardNavigation.DirectionalNavigation="Cycle" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Flat dark menu item with a steel-blue checkmark column. Click
       behavior (IsCheckable toggle, Command, StaysOpenOnClick) lives on
       the MenuItem class itself, so this template only handles visuals.
       Assumes flat menus (no submenus): true for every menu this app
       builds. -->
  <Style x:Key="DarkMenuItem" TargetType="MenuItem">
    <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="MenuItem">
          <Border x:Name="Bg" Background="Transparent" CornerRadius="4" Padding="8,6">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <TextBlock x:Name="CheckGlyph"
                         Grid.Column="0"
                         Text="&#x2713;"
                         FontWeight="Bold"
                         Foreground="{StaticResource DarkAccentBrush}"
                         VerticalAlignment="Center"
                         Visibility="Hidden" />
              <ContentPresenter Grid.Column="1"
                                ContentSource="Header"
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True" />
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
              <Setter TargetName="Bg" Property="Background" Value="#3d3d3d" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.45" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="DarkMenuSeparator" TargetType="Separator">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Separator">
          <Rectangle Height="1" Fill="#444444" Margin="6,4" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
